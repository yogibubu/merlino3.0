#!/usr/bin/env bash
set -euo pipefail

FC="${FC:-gfortran}"
BUILD_DIR="build"
LOG="$BUILD_DIR/gf_build.log"

mkdir -p "$BUILD_DIR"
rm -f "$LOG"

"$FC" \
  -std=legacy \
  -w \
  -c gf.f \
  -o "$BUILD_DIR/gf.o" \
  >"$LOG" 2>&1

echo "Built fortran/harmonic_internal/build/gf.o"
