[project]
name = "merlino-fit"
version = "0.0.1"
description = "Robust PES fit prototype"
requires-python = ">=3.9"
dependencies = ["numpy"]

[project.scripts]
merlino-fit = "survibfit.cli:main"
merlino-similarity = "survibfit.synthon_similarity:main"

[tool.pytest.ini_options]
cache_dir = "/tmp/pytest-cache"
